import { addTodo, listTodos } from './todos'
import { createConversation, getConversation } from './conversations'

export default {
  listTodos,
  addTodo,
  createConversation,
  getConversation,
}
